#!/usr/bin/with-contenv bash
set -euo pipefail
source /usr/local/bin/runtime-env.sh
while [[ ! -S /run/seatd.sock || ! -S "${XDG_RUNTIME_DIR}/bus" ]]; do sleep 0.25; done
exec s6-setuidgid abc labwc -C /config/labwc
